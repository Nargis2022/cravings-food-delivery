'use client';
import Image from 'next/image';
import { Minus, Plus, Trash2 } from 'lucide-react';
import { CartLine, useCart } from './CartContext';
export default function CartItem({ item }: { item: CartLine }) { const { increase, decrease, remove } = useCart(); return <div className="flex gap-4 rounded-2xl border border-[#eadfd7] bg-white p-4"><div className="relative h-24 w-24 shrink-0 overflow-hidden rounded-xl bg-[#f7eee7]"><Image src={item.image} alt={item.name} fill sizes="96px" className="object-cover" /></div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h3 className="font-extrabold text-[#292724]">{item.name}</h3><p className="mt-1 line-clamp-1 text-xs text-[#827870]">{item.description}</p></div><span className="font-extrabold text-[#f4512a]">${(item.price * item.quantity).toFixed(2)}</span></div><div className="mt-4 flex items-center justify-between"><div className="flex items-center gap-3 rounded-full bg-[#fff7f1] p-1"><button aria-label="Decrease quantity" onClick={() => decrease(item.id)} className="grid h-7 w-7 place-items-center rounded-full text-[#756a62] hover:bg-white"><Minus size={13} /></button><span className="w-3 text-center text-sm font-bold">{item.quantity}</span><button aria-label="Increase quantity" onClick={() => increase(item.id)} className="grid h-7 w-7 place-items-center rounded-full bg-[#f4512a] text-white hover:bg-[#df421f]"><Plus size={13} /></button></div><button onClick={() => remove(item.id)} className="flex items-center gap-1 text-xs font-bold text-[#a49a93] hover:text-[#e34b2b]"><Trash2 size={14} /> Remove</button></div></div></div>; }
