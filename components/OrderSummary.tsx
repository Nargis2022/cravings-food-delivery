'use client';
import { ArrowRight, LockKeyhole } from 'lucide-react';
export default function OrderSummary({ subtotal }: { subtotal: number }) { const delivery = subtotal > 35 ? 0 : 3.95; const tax = subtotal * .0825; const total = subtotal + delivery + tax; return <aside className="rounded-[24px] bg-[#2a2926] p-7 text-white shadow-[0_20px_40px_rgba(42,41,38,.18)]"><h2 className="text-xl font-extrabold">Order summary</h2><div className="mt-7 space-y-4 text-sm text-[#cfc6be]"><div className="flex justify-between"><span>Subtotal</span><span className="font-semibold text-white">${subtotal.toFixed(2)}</span></div><div className="flex justify-between"><span>Delivery fee</span><span className="font-semibold text-white">{delivery === 0 ? 'Free' : `$${delivery.toFixed(2)}`}</span></div><div className="flex justify-between"><span>Estimated tax</span><span className="font-semibold text-white">${tax.toFixed(2)}</span></div></div><div className="my-6 border-t border-white/10" /><div className="flex items-end justify-between"><span className="font-bold">Total</span><span className="text-3xl font-extrabold text-[#ff845e]">${total.toFixed(2)}</span></div><button onClick={() => alert('Checkout is ready for your next step.')} className="mt-7 flex w-full items-center justify-center gap-2 rounded-full bg-[#f4512a] py-4 text-sm font-extrabold text-white transition hover:bg-[#df421f]">Continue to checkout <ArrowRight size={17} /></button><p className="mt-4 flex items-center justify-center gap-1 text-center text-[11px] text-[#a9a09a]"><LockKeyhole size={12} /> Secure checkout, always.</p></aside>; }
